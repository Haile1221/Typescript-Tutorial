type CourseStatus = Record<string, boolean>

const courses :CourseStatus ={
    typeScript : true,
    React : true,
    Nodejs : true,
    Aws :true,
    mongoDB : true
};

// output 
console.log(courses);

