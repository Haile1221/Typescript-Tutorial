function add(a: number, b: number): number {
    return a + b;
}

const result = add(20, 30);
console.log("Result :", result);

function greet(name: string): string {
return `Hello , ${name}!!`
}

console.log(greet("Beshilo"))